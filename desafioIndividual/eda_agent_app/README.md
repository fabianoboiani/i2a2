# EDA Agent – Codegen Only

Somente modo de geração de código: a LLM gera Python, sandbox executa, capturamos texto (RESULT_TEXT) e gráficos.

## Rodando
poetry install
poetry run streamlit run app.py